# Vercel Postgres: injected automatically by Vercel when Storage > Postgres is linked.
# Fallback to DATABASE_URL for local dev or other providers.
POSTGRES_URL="postgresql://postgres:A12345fe@localhost:5432/barbershop?schema=public"

# Admin password for the dashboard
ADMIN_PASSWORD="JalissonCorts2026"
