# Neon (produção) — usar a URL direta (sem &pgbouncer=true)
# O driver adapter (@prisma/adapter-pg) já faz pooling via pg.Pool
DATABASE_URL="postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/barbershop?sslmode=require"

# Local (desenvolvimento)
# DATABASE_URL="postgresql://postgres:A12345fe@localhost:5432/barbershop?schema=public"

# Senha do admin para o painel
ADMIN_PASSWORD="Admin456"
